backoff-strategy:
  backoff-time-seconds: -1,7

kafka:

  internal:
    truststore-password: SeCrEt-007!

  external:
    bootstrap-servers: external.kafka-host.org:443
    truststore-path: demo-truststore.p12

  this-is-valid:
    bootstrap-servers: external.kafka-host.org:443

  This-ain't_valid:
    bootstrap-servers: external.kafka-host.org:443

routes:

  example-123:
    source: nope
    source-topic: (123

  Example 123:
    target: yodel
    target-topic: ((.*)